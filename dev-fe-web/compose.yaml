networks:
  roomx_network:
    name: roomx_network
    external: true

services:
  roomx-fe:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4173:4173"
    env_file:
      - .env
    restart: always
    networks:
      - roomx_network